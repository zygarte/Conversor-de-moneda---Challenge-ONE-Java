

public class CurrencyConverter {

 

    // Métodos de conversión a Dólar
    static double convertToDollar(double amount) {

        
        // Implementa la conversión a Dólar aquí

        return amount * 0.00024;
    }

    // Métodos de conversión a Euros
    static double convertToEuro(double amount) {
        // Implementa la conversión a Euros aquí
        return amount * 0.00022;
    }

    // Métodos de conversión a Libras Esterlinas
    static double convertToPound(double amount) {
        // Implementa la conversión a Libras Esterlinas aquí
        return amount * 0.00019;
    }

    // Métodos de conversión a Yen Japonés
     static double convertToYen(double amount) {
        // Implementa la conversión a Yen Japonés aquí
        return amount * 0.034;
    }

    // Métodos de conversión a Won sul-coreano
    static double convertToWon(double amount) {
        // Implementa la conversión a Won sul-coreano aquí
        return amount * 0.31;
    }

    // Métodos de conversión desde Dólar
   static double convertFromDollar(double amount) {
        // Implementa la conversión desde Dólar aquí
        return amount * 4159.25;
    }

    // Métodos de conversión desde Euros
     static double convertFromEuro(double amount) {
        // Implementa la conversión desde Euros aquí
        return amount * 4565.40;
    }

    // Métodos de conversión desde Libras Esterlinas
    static double convertFromPound(double amount) {
        // Implementa la conversión desde Libras Esterlinas aquí
        return amount * 5340.89;
    }

    // Métodos de conversión desde Yen Japonés
  static double convertFromYen(double amount) {
        // Implementa la conversión desde Yen Japonés aquí
        return amount * 29.27;
    }

    // Métodos de conversión desde Won sul-coreano
    static double convertFromWon(double amount) {
        // Implementa la conversión desde Won sul-coreano aquí
        return amount * 3.20;
    }
}
